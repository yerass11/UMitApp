import SwiftUI

struct PaymentMethodView: View {
    var body: some View {
        VStack {
            Text("Payment Methods")
                .font(.title)
                .padding()
            Text("This is where the payment methods will go.")
        }
    }
}
